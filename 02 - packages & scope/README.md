# Packages & Scope

 - 3 levels of scope:

     - block
     - package
     - global

In Go, a name is exported if it begins with a capital letter.

## Create a go module

    go mod init example.com/module-name
